{
  "name": "todo-list-ts",
  "version": "1.0.0",
  "description": "todo list by ts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "tsc -w"
  },
  "author": "awesome jeong",
  "license": "ISC",
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
